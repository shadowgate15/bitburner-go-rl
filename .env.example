DOCKER_PROJECT_NAME=python-boilerplate
DOCKER_NETWORK_NAME=project-network
DOCKER_IMAGE_TAG=dev
DOCKER_CONTAINER_NAME=dev

APP_STAGE=development

# CLI arguments forwarded to `python __main__.py` in the train container.
# Example: TRAIN_ARGS=--board-size 9 --total-frames 200000
TRAIN_ARGS=